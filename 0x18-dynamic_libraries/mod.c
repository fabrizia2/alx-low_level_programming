#include "main.h"

/**
 * mod - divides two int and returns remainder
 *
 * @a: first integer
 * @b: second integer
 * 
 * Return: sum of the two integers
 */
int mod(int a, int b)
{
	int mod;
	mod = a % b;
	return (mod);
}
